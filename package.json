{
    "name": "bloodpressure",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "seed:hash": "node scripts/hash-password.mjs"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.8.19",
        "vitest": "~3.2.0",
        "wrangler": "^4.51.0"
    },
    "overrides": {
        "esbuild": "0.25.12"
    }
}
